# نسخة أون لاين للتطوير

تاريخ إنشاء النسخة: 16 يونيو 2026.

هذه نسخة تطوير مستقلة مأخوذة من النسخة المحلية المستقرة لنظام التصاريح.
النسخة المحلية تبقى مخصصة حاليًا للأنظمة الحكومية والتشغيل داخل الشبكة.

## العزل الحالي

- رابط التطوير: `http://127.0.0.1:5001`
- قاعدة التطوير: `.online-storage/.localdata/online-development.db`
- الملفات المرفوعة: `.online-storage/uploads`
- النسخ الاحتياطية: `.online-storage/backups`
- الإعدادات التشغيلية: `.online-storage/config`
- لا تستخدم قاعدة النسخة المثبتة على المنفذ `5000`.
- لم يتم نسخ المستخدمين أو التصاريح أو الزيارات أو كلمات المرور من النسخة المحلية.

## التشغيل

شغّل الملف:

`تشغيل نسخة اون لاين.ps1`

أو نفذ:

```powershell
$env:ASPNETCORE_ENVIRONMENT = "Development"
$env:ASPNETCORE_URLS = "http://127.0.0.1:5001"
$env:VehiclePermitSystemWeb__StorageRoot = ".online-storage"
dotnet run --no-launch-profile
```

عند أول تشغيل تظهر شاشة التهيئة لإنشاء جهة وحساب مالك جديدين للبيئة التجريبية.

## خطة التطوير المعتمدة

1. تثبيت النسخة المحلية وعدم خلط تطويرها اليومي مع المنتج السحابي.
2. تشغيل نسخة الإنتاج على PostgreSQL مشتركة مع عزل جميع السجلات بواسطة `TenantId`.
3. استخدام دومين موحد للمنصة، ورابط مستقل لكل جهة بالصيغة `/o/{slug}`.
4. إدارة الجهات والاشتراكات مركزيًا دون حدود على المستخدمين أو التصاريح.
5. توفير PWA قابلة للتثبيت على الجوال مع دعم الكاميرا والباركود عبر HTTPS.
6. عدم تخزين صفحات الدخول أو بيانات التصاريح والزيارات للاستخدام دون اتصال.
7. إعداد نشر Linux ووكيل عكسي وHTTPS وHealth Checks ونسخ احتياطي لـ PostgreSQL.
8. إضافة MFA وإدارة أسرار ومراقبة وتنبيهات قبل الإطلاق العام.

المرجع التفصيلي: `docs/product-vision-and-roadmap-ar.md`.

دليل نسخة الجوال: `docs/mobile-pwa-ar.md`.
