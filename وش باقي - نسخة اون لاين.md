# وش باقي - نسخة الأون لاين

هذا الملف مختصر لنقل السياق إلى محادثة ثانية عن نسخة الأون لاين من نظام التصاريح.

## الحالة الحالية

- مسار النسخة:
  `C:\Users\hp\Desktop\المشاريع\نسخة اون لاين للتطوير`
- الفرع:
  `online-development`
- المستودع:
  `https://github.com/saker911/online-development`
- التشغيل المحلي:
  `http://127.0.0.1:5001`
- سكربت التشغيل:
  `scripts\run-online-5001.ps1`
- أمر تشغيل بالخلفية:
  ```powershell
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-online-5001.ps1 -NoBuild -Detached
  ```

## آخر ما تم

- تقليل بيانات الهوية في نسخة الأون لاين.
- استبدال ظهور `رقم الهوية` في الواجهات والتقارير بـ `المعرف الداخلي`.
- تنظيف واجهات نسخة الأون لاين من النصوص الحساسة مثل السجل المدني ورقم الهوية.
- تنظيف اسم شخصي من بيانات التطوير، وصار يظهر `مالك النظام`.
- تبسيط نموذج الزيارات:
  - الخيارات الحالية: `مضيف`، `موظف`، `جهة أخرى`.
  - إزالة خيارات `موقوف / نزيل / سجين` من نموذج الحجز.
  - إيقاف الاستنتاج التلقائي من نص الغرض.
- إضافة سكربت تشغيل موثوق للمنفذ `5001`.
- بدء أساس عزل الجهات `Tenant Isolation`:
  - إضافة كيان `Tenant` وسياق tenant للطلبات.
  - إضافة `TenantId` للكيانات الأساسية: المستخدمون، التصاريح، الزيارات، الإعدادات، الأقسام، التفويضات، السجلات، وشاشات العرض.
  - إضافة فلترة تلقائية حسب الجهة داخل `ApplicationDbContext`.
  - تجهيز مايغريشن PostgreSQL باسم `AddTenantIsolationFoundation`.
- بدء لوحة مالك النظام لإدارة الجهات:
  - إضافة خدمة `TenantManagementService`.
  - إضافة شاشة `Tenants` لعرض الجهات وإضافة/تعديل/تفعيل/إيقاف الجهة.
  - ربط الشاشة في القائمة الجانبية لمالك النظام فقط.
  - تعديل قراءة إعدادات الإدارة لتعمل كسجل مستقل لكل جهة بدل الاعتماد الصارم على `Id = 1`.
  - دعم اختيار الجهة قبل تسجيل الدخول عبر معامل `tenant` في رابط الدخول، وتثبيت الجهة في claim بعد الدخول.
  - ربط إنشاء وتعديل المستخدمين بجهة محددة من شاشة المستخدمين لمالك النظام.
  - عرض الجهة داخل فهرس المستخدمين، ودعم قراءة/تفعيل/إعداد مستخدمي الجهات الأخرى لمالك النظام فقط.
  - جعل مايغريشن PostgreSQL لعزل الجهات آمناً عند وجود أعمدة `TenantId` مسبقاً في قاعدة محدثة جزئياً.
- توسيع لوحة مالك النظام لتصبح بداية لوحة منصة SaaS:
  - إضافة حالة اشتراك لكل جهة: تجريبي، نشط، موقوف، منتهي.
  - إضافة اسم الباقة والدومين الأساسي.
  - إضافة حدود الباقة: المستخدمون، التصاريح الشهرية، الزيارات الشهرية.
  - منع تسجيل الدخول للجهة إذا كانت موقوفة أو منتهية أو انتهت تجربتها.
  - منع إضافة مستخدم جديد إذا وصلت الجهة إلى حد المستخدمين.
  - منع إنشاء تصاريح أو زيارات جديدة إذا وصلت الجهة إلى الحد الشهري.
  - رابط جهة موحد داخل دومين المنصة بالصيغة `/o/{slug}` مع تثبيت الجهة في جلسة الدخول.
- آخر commits مهمة:
  - `1085d22 feat: simplify online visit subject types`
  - `c01523e chore: add reliable online dev runner`
  - `a6401e2 feat: clean sensitive identity labels in online UI`
  - `14018f0 feat: relabel report identity fields for online mode`
  - `f3ed89f feat: minimize sensitive identity fields for online edition`

## ملاحظة مهمة قبل المتابعة

يوجد حاليًا تعديلان غير محفوظين في:

- `Views/Shared/_Layout.cshtml`
- `wwwroot/css/theme-vars.css`

قبل بدء شغل جديد، راجع هل هذه التعديلات مقصودة واحفظها أو افصلها عن شغل نسخة الأون لاين.

## المتبقي المقترح

1. **مراجعة بصرية كاملة**
   - افتح الصفحات الأساسية على `5001`.
   - تأكد من التنسيق بعد تغيير النصوص.
   - الصفحات المهمة:
     - الرئيسية
     - التصاريح
     - إنشاء تصريح
     - الزيارات
     - إنشاء زيارة
     - المستخدمون والصلاحيات
     - الإدارة
     - التقارير
     - شاشات العرض
     - ماسح الباركود

2. **عزل الجهات والشركات**
   - تم تنفيذ الأساس البرمجي الأولي للعزل.
   - تم تنفيذ لوحة أولية لإدارة الجهات والاشتراكات.
   - تم اختبار عزل المستخدمين بين جهتين.
   - المتبقي:
     - مراجعة العزل بصريًا ووظيفيًا في التصاريح، الزيارات، التقارير، التفويضات، الأقسام، وشاشات العرض.
     - اختبار حدود الباقات على بيانات واقعية أكثر.

3. **تحويل نسخة الأون لاين إلى منتج SaaS**
   - تحديد مفهوم الجهة أو المستأجر `Tenant`.
   - تم اعتماد رابط جهة ثابت داخل دومين المنصة بالصيغة `/o/{slug}`.
   - تم تجهيز لوحة مالك النظام لإضافة الجهات وإدارتها.
   - تم ربط إنشاء وتعديل المستخدمين بجهة محددة من لوحة مالك النظام.
   - تم إضافة حقول الاشتراك وإلغاء حدود الاستخدام والدومينات المخصصة.
   - تم تجهيز نسخة PWA قابلة للتثبيت على الجوال دون تخزين البيانات الحساسة دون اتصال.
   - المتبقي:
     - صفحة منفصلة أكثر تخصصًا للفوترة والمدفوعات عند الحاجة.

4. **قاعدة البيانات والإنتاج**
   - الاعتماد على PostgreSQL.
   - مراجعة الفهارس.
   - تجهيز نسخة احتياطية واستعادة.
   - تجهيز سكربتات نشر وترحيل آمنة.

5. **الأمان**
   - مراجعة الصلاحيات بين الجهات.
   - منع تسريب بيانات جهة لجهة أخرى.
   - مراجعة ملفات الإعداد والأسرار.
   - مراجعة الكوكيز و HTTPS و CSRF.
   - اختبار مستخدم بصلاحيات محدودة.

6. **النشر على سيرفر**
   - تجهيز Nginx.
   - SSL لدومين المنصة الموحد.
   - تشغيل الخدمة عبر `systemd` أو Docker.
   - إعداد health check.
   - إعداد logs واضحة.

7. **اختبار ضغط خفيف**
   - تسجيل دخول أكثر من مستخدم.
   - إنشاء زيارات وتصاريح متعددة.
   - فحص التقارير.
   - فحص استهلاك الذاكرة والمعالج.

8. **ملف عرض للمدير أو العميل**
   - وصف البرنامج.
   - المميزات.
   - صور للشاشات.
   - التقنية المستخدمة:
     - ASP.NET Core MVC
     - PostgreSQL
     - Bootstrap/CSS
     - Nginx عند النشر
   - طريقة عمله على شبكة داخلية أو سيرفر.

## أفضل خطوة قادمة

راجع التعديلات الحالية ثم احفظها في GitHub، وبعدها ابدأ مراجعة العزل في التصاريح والزيارات والتقارير قبل تجهيز النشر والدومين.
